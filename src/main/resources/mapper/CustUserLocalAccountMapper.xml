<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.com.glsx.shield.modules.mapper.CustUserLocalAccountMapper">
    <resultMap id="BaseResultMap" type="cn.com.glsx.shield.modules.entity.CustUserLocalAccount">
        <!--
          WARNING - @mbg.generated
        -->
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="auth_id" jdbcType="INTEGER" property="authId"/>
        <result column="union_id" jdbcType="VARCHAR" property="unionId"/>
        <result column="username" jdbcType="VARCHAR" property="username"/>
        <result column="avatar" jdbcType="VARCHAR" property="avatar"/>
        <result column="del_flag" jdbcType="TINYINT" property="delFlag"/>
        <result column="created_date" jdbcType="TIMESTAMP" property="createdDate"/>
        <result column="created_by" jdbcType="INTEGER" property="createdBy"/>
        <result column="updated_date" jdbcType="TIMESTAMP" property="updatedDate"/>
        <result column="updated_by" jdbcType="INTEGER" property="updatedBy"/>
    </resultMap>
    <sql id="Base_Column_List">
        <!--
          WARNING - @mbg.generated
        -->
        id, auth_id, union_id, username, avatar, del_flag, created_date, created_by, updated_date,
        updated_by
    </sql>

    <select id="selectByUserIds" resultType="cn.com.glsx.usercenter.api.dto.response.CUser">
        select tcuar.user_id userId, tcu.rel_user_id relUserId, tcula.account, tcula2.username, tcula2.avatar
        from t_cust_user_auth_rel tcuar
        inner join t_cust_user_local_auth tcula on tcuar.auth_id = tcula.id
        left join t_cust_user tcu on tcuar.user_id = tcu.id
        left join t_cust_user_local_account tcula2 on tcuar.auth_id = tcula2.auth_id
        where tcuar.del_flag = 0
        and tcuar.auth_type = 'local'
        and tcuar.user_id in
        <foreach collection="cuserIds" item="item" index="index" open="(" close=")" separator=",">
            #{item}
        </foreach>
    </select>

    <select id="selectByRelUserIds" resultType="cn.com.glsx.usercenter.api.dto.response.CUser">
        select tcuar.user_id userId, tcu.rel_user_id relUserId, tcula.account, tcula2.username, tcula2.avatar
        from t_cust_user_auth_rel tcuar
        inner join t_cust_user_local_auth tcula on tcuar.auth_id = tcula.id
        inner join t_cust_user tcu on tcuar.user_id = tcu.id
        left join t_cust_user_local_account tcula2 on tcuar.auth_id = tcula2.auth_id
        where tcuar.del_flag = 0 and tcu.del_flag = 0
        and tcuar.auth_type = 'local'
        and tcu.rel_user_id in
        <foreach collection="relUserIds" item="item" index="index" open="(" close=")" separator=",">
            #{item}
        </foreach>
    </select>
</mapper>